feature_id: m80_extension_cards
claim: |
  M-80: official provider / target / dialect cards land under
  ``python/xpu_rt/{providers,targets,dialects}/cards/*.yaml``.

  Shipped inventory:

  * **19 provider cards** (1 over the original 18 — adds the
    existing ``claude_kernel`` agent-driven provider).
    paper_claimable=true only on ``cffi_c`` (promote) and ``triton``
    (promote); every other external-SDK provider stays at
    ``probe``. ``python_reference`` is at ``verify`` (correctness
    fallback, not paper-claimable). ``autocomp``, ``kernelblaster``,
    ``claude_kernel`` at ``generate``.
  * **13 target cards** spanning host_cpu, cuda_sm75/80/90,
    rocm_gpu, aws_neuron_trn/inf, google_tpu_v5e/v6e, gemmini_mx,
    radiance_muon, hexagon_npu, saturn_opu.
  * **10 dialect provider cards** for cuda_tile_ir, hexagon_mlir,
    pallas, nki, exo, gemmini_c, radiance_muon, iree, triton_mlir,
    stablehlo.

  ``python/xpu_rt/providers/card_loader.py`` discovers cards and
  exposes ``iter_provider_cards``, ``iter_target_cards``,
  ``iter_dialect_cards``, ``load_all_cards``.

  Hard contract enforced by ``tests/providers/test_card_inventory.py``:

  1. Every card under the three roots loads via the M-79 schema.
  2. The complete Phase F roadmap inventory is present (no missing
     IDs).
  3. ``paper_claimable: true`` only at ``integration_level`` of
     ``verify`` or ``promote`` (hard rule 6).
  4. Every SDK-gated provider declares non-empty ``required_env`` /
     ``required_python_imports`` / ``required_commands`` so the
     M-81 probe can block honestly (no silent disappearance).
  5. The verify/promote count never outweighs probe/generate at
     a 2:3 ratio — catches paper-claim inflation drift.
  6. Every ``target_family`` a provider references has a matching
     ``TargetCard`` (modulo synthetic ``custom_accelerator`` /
     ``riscv_opu`` catch-alls).

  Tests (12/12 green):

  * ``tests/providers/test_card_inventory.py``

  Vendoring confirmed forbidden: every external-SDK card uses
  env vars (``CUDA_TILE_ROOT``, ``HEXAGON_MLIR_ROOT``,
  ``CUTLASS_ROOT``, ``THUNDERKITTENS_ROOT``, ``CHIPYARD_ROOT``,
  ``GEMMINI_ROOT``, ``RADIANCE_KERNELS_ROOT``,
  ``RISCV_TOOLCHAIN_PATH``, ``NEURON_HOME``, ``AUTOCOMP_ROOT``,
  ``KERNELBLASTER_ROOT``, ``KERNELBENCH_ROOT``, ``CAESAR_ROOT``,
  ``MIRAGE_ROOT``, ``TILELANG_ROOT``, ``IREE_ROOT``) — never a
  vendored checkout.
realness_level: production_path
forbidden:
  - card without integration_level
  - card with paper_claimable=true at integration_level <= probe
  - vendoring external repositories
  - declaring a status outside the typed enum
  - SDK-gated provider with empty required_env/imports/commands
  - paper-claim inflation (verify+promote outweighing probe+generate)
required_evidence:
  - python/xpu_rt/providers/cards/cffi_c.yaml
  - python/xpu_rt/providers/cards/python_reference.yaml
  - python/xpu_rt/providers/cards/triton.yaml
  - python/xpu_rt/providers/cards/autocomp.yaml
  - python/xpu_rt/providers/cards/kernelblaster.yaml
  - python/xpu_rt/providers/cards/exo.yaml
  - python/xpu_rt/providers/cards/tilelang.yaml
  - python/xpu_rt/providers/cards/cutlass_cute.yaml
  - python/xpu_rt/providers/cards/bitblas.yaml
  - python/xpu_rt/providers/cards/thunderkittens.yaml
  - python/xpu_rt/providers/cards/mirage.yaml
  - python/xpu_rt/providers/cards/kernelbench_caesar.yaml
  - python/xpu_rt/providers/cards/cuda_tile_ir.yaml
  - python/xpu_rt/providers/cards/hexagon_mlir.yaml
  - python/xpu_rt/providers/cards/pallas.yaml
  - python/xpu_rt/providers/cards/nki.yaml
  - python/xpu_rt/providers/cards/gemmini_c.yaml
  - python/xpu_rt/providers/cards/radiance_muon.yaml
  - python/xpu_rt/providers/cards/claude_kernel.yaml
  - python/xpu_rt/targets/cards/host_cpu.yaml
  - python/xpu_rt/targets/cards/cuda_sm75.yaml
  - python/xpu_rt/targets/cards/cuda_sm80.yaml
  - python/xpu_rt/targets/cards/cuda_sm90.yaml
  - python/xpu_rt/targets/cards/rocm_gpu.yaml
  - python/xpu_rt/targets/cards/aws_neuron_trn.yaml
  - python/xpu_rt/targets/cards/aws_neuron_inf.yaml
  - python/xpu_rt/targets/cards/google_tpu_v5e.yaml
  - python/xpu_rt/targets/cards/google_tpu_v6e.yaml
  - python/xpu_rt/targets/cards/gemmini_mx.yaml
  - python/xpu_rt/targets/cards/radiance_muon.yaml
  - python/xpu_rt/targets/cards/hexagon_npu.yaml
  - python/xpu_rt/targets/cards/saturn_opu.yaml
  - python/xpu_rt/dialects/cards/cuda_tile_ir.yaml
  - python/xpu_rt/dialects/cards/hexagon_mlir.yaml
  - python/xpu_rt/dialects/cards/pallas.yaml
  - python/xpu_rt/dialects/cards/nki.yaml
  - python/xpu_rt/dialects/cards/exo.yaml
  - python/xpu_rt/dialects/cards/gemmini_c.yaml
  - python/xpu_rt/dialects/cards/radiance_muon.yaml
  - python/xpu_rt/dialects/cards/iree.yaml
  - python/xpu_rt/dialects/cards/triton_mlir.yaml
  - python/xpu_rt/dialects/cards/stablehlo.yaml
  - python/xpu_rt/providers/card_loader.py
  - tests/providers/test_card_inventory.py
commit: 35ee97f497bc69c0716c25b935f575c3cd7d63f4
created_at_utc: 2026-05-11T00:00:00Z
notes: |
  Phase F-2. 19+13+10 = 42 cards. Adapter implementations for
  most providers land in M-82 and beyond — cards-only is honest
  here: the probe (M-81) will report ``blocked`` for every
  external SDK that isn't installed in the current environment.
