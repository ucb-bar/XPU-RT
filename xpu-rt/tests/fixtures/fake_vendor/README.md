# FakeVendorMLIR

A minimal third-party MLIR dialect used to exercise CompGen's
vendor-dialect integration agent in tests. Not a real vendor.

## Build

```bash
./tools/fake-opt --help   # not implemented; the scanner finds it
```
