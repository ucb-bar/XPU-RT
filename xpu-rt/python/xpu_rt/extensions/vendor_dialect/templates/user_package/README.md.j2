# {{ package_name }}

CompGen vendor adapter for the **{{ vendor_name }}** MLIR dialect.

Generated by `compgen.extensions.vendor_dialect.scaffold`. Edit freely;
the frozen spec lives at `{{ package_name }}/descriptor.yaml`.

## Install

```bash
pip install -e .
```

## Use

```python
from compgen.api import compile_with_vendor
from compgen.extensions.vendor_dialect import get_adapter

adapter = get_adapter("{{ vendor_name }}")
compile_with_vendor(model, adapter, target="{{ target }}", sample_inputs=...)
```

## Layout

* `{{ package_name }}/adapter.py`   — `{{ adapter_class_name }}({{ adapter_class_name }}Base)`
* `{{ package_name }}/lowering.py`  — Payload IR → {{ vendor_name }} dialect
{% if kernel_authoring_required %}
* `{{ package_name }}/kernels.py`   — LLM-backed kernel provider
{% endif %}
* `{{ package_name }}/bundle.py`    — vendor toolchain driver
* `{{ package_name }}/descriptor.yaml` — frozen integration spec

License: {{ license or "see upstream vendor repo" }}.
