//===- {{ prefix }}Passes.td - {{ prefix }} dialect passes ---*- tablegen -*-===//
//
// Auto-generated by CompGen mlir_cppgen — do not edit manually.
//
//===----------------------------------------------------------------------===//

#ifndef {{ prefix | upper }}_PASSES
#define {{ prefix | upper }}_PASSES

include "mlir/Pass/PassBase.td"

{% for p in passes %}
def {{ p.td_name }} : Pass<"{{ p.pass_flag }}", "::mlir::ModuleOp"> {
    let summary = "{{ p.summary }}";
    let description = [{
        {{ p.description }}
    }];
}

{% endfor %}
#endif // {{ prefix | upper }}_PASSES
