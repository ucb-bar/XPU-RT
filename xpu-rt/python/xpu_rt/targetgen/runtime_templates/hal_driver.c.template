/*
 * HAL Driver for {target_name}
 *
 * Auto-generated by CompGen targetgen.
 * Target family : {family}
 * Dispatch model: {dispatch_model}
 *
 * DO NOT EDIT — regenerate from the hardware spec.
 */

#include <stddef.h>
#include <stdint.h>

{extra_includes}

/* ---- Forward declarations from sub-modules ---- */

/* Allocator */
void *hal_alloc(size_t size_bytes);
void  hal_free(void *ptr);

/* Dispatch */
int hal_dispatch_kernel(const char *kernel_name,
                        const void *args, size_t args_size);

/* Synchronization */
int hal_sync(void);

/* ---- VTable ---- */

typedef struct {{
    void *(*alloc)(size_t);
    void  (*free)(void *);
    int   (*dispatch)(const char *, const void *, size_t);
    int   (*sync)(void);
}} hal_vtable_t;

static const hal_vtable_t vtable = {{
    .alloc    = hal_alloc,
    .free     = hal_free,
    .dispatch = hal_dispatch_kernel,
    .sync     = hal_sync,
}};

const hal_vtable_t *hal_get_vtable(void) {{
    return &vtable;
}}

/* ---- Lifecycle ---- */

int hal_init(void) {{
{init_body}
}}

int hal_shutdown(void) {{
{shutdown_body}
}}
