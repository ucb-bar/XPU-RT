schema_version: provider_card_v1
provider_id: bitblas
integration_level: probe
target_families: [cuda]
contract_kinds: [matmul]
emits: [triton_source, cubin]
entrypoint: compgen.providers.adapters.bitblas:BitBlasProvider
paper_claimable: false
required_env: []
required_commands: []
required_python_imports: [bitblas]
description: |
  BitBLAS quantized-matmul provider. Card + probe only; adapter
  lands when the M-82 normalization pattern is exercised. Probe
  blocks with ``python_package_missing`` until BitBLAS is
  installed.
