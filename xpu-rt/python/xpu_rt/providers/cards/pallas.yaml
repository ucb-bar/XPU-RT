schema_version: provider_card_v1
provider_id: pallas
integration_level: probe
target_families: [tpu]
contract_kinds: [matmul, pointwise, attention, fused_region]
emits: [pallas_source, mlir]
entrypoint: compgen.providers.adapters.pallas:PallasProvider
paper_claimable: false
required_env: []
required_commands: []
required_python_imports: [jax, jax.experimental.pallas]
description: |
  Pallas kernel provider for Google TPU (JAX backend). Card +
  probe only; blocks with ``python_package_missing`` when JAX or
  Pallas are unavailable. Hardware-backed claims require TPU
  access at evidence-pack time.
