schema_version: provider_card_v1
provider_id: hexagon_mlir
integration_level: probe
target_families: [hexagon_npu]
contract_kinds: [matmul, pointwise, fused_region, attention]
emits: [hexagon_mlir, elf]
entrypoint: compgen.providers.adapters.hexagon_mlir:HexagonMLIRProvider
paper_claimable: false
required_env: [HEXAGON_MLIR_ROOT]
required_commands: []
required_python_imports: []
description: |
  Hexagon-MLIR provider for Qualcomm Hexagon NPUs (HVX, TCM, DMA,
  experimental matrix support). Open-source AI-compiler stack
  integrated via env-var; not vendored. Blocks with ``env_missing``
  until ``HEXAGON_MLIR_ROOT`` is set.
