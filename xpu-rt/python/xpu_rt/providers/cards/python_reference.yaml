schema_version: provider_card_v1
provider_id: python_reference
integration_level: verify
target_families: [host_cpu]
contract_kinds: [matmul, pointwise, fused_region, reduction, attention]
emits: [python_source]
entrypoint: compgen.kernels.providers.c_reference:CReferenceProvider
paper_claimable: false
required_env: []
required_commands: []
required_python_imports: [numpy]
description: |
  Pure-Python reference provider. Emits a NumPy implementation that
  is correct-by-construction but not performance-claimable. Useful
  as a differential reference when CFFI cannot be compiled (e.g.
  inside a minimal sandbox).
