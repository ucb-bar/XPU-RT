schema_version: provider_card_v1
provider_id: autocomp
integration_level: generate
target_families: [cuda, custom_accelerator]
contract_kinds: [matmul, pointwise, fused_region, attention]
emits: [triton_source, c_source]
entrypoint: compgen.kernels.providers.autocomp:AutocompProvider
paper_claimable: false
required_env: [AUTOCOMP_ROOT]
required_commands: []
required_python_imports: [autocomp]
description: |
  Autocomp LLM-guided kernel search provider. Integrated through
  ``third_party/autocomp`` (editable install) and the
  ``compgen.kernels.autocomp_adapter`` shim. paper_claimable=false
  because the search loop's "best" kernel still passes through the
  same verifier path as any other provider.
