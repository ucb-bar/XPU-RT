schema_version: provider_card_v1
provider_id: tilelang
integration_level: probe
target_families: [cuda, rocm, host_cpu]
contract_kinds: [matmul, pointwise, fused_region]
emits: [tilelang_source, ptx]
entrypoint: compgen.providers.adapters.tilelang:TileLangProvider
paper_claimable: false
required_env: [TILELANG_ROOT]
required_commands: []
required_python_imports: [tilelang]
description: |
  TileLang DSL provider. Card-level declaration; adapter
  implementation lands when the M-82 normalization pattern proves
  itself on cffi-C / Triton. Probe blocks with
  ``python_package_missing`` until TileLang is installed.
