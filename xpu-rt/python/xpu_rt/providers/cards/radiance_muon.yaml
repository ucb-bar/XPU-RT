schema_version: provider_card_v1
provider_id: radiance_muon
integration_level: probe
target_families: [radiance, custom_accelerator]
contract_kinds: [matmul, pointwise]
emits: [radiance_kernel_source, radiance_elf, soc_elf]
entrypoint: compgen.providers.adapters.radiance_muon:RadianceMuonProvider
paper_claimable: false
required_env: [RADIANCE_KERNELS_ROOT, RISCV_TOOLCHAIN_PATH]
required_commands: []
required_python_imports: []
description: |
  Radiance / Muon SIMT-kernel provider for the Radiance / Chipyard
  research accelerator. Emits ``.radiance.elf`` / ``.soc.elf``
  artifacts. Blocks with ``env_missing`` until both env vars are
  set.
