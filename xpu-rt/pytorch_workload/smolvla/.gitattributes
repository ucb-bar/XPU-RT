# Git LFS configuration for SmolVLA ONNX models
# Use this if models are stored in the repository

# Track large MLIR files with Git LFS
*.mlir filter=lfs diff=lfs merge=lfs -text

# Track ONNX files if any
*.onnx filter=lfs diff=lfs merge=lfs -text
