name: iree_cpu_events
version: "0.1.0"
kinds: ["ProfilerPack"]
owned_surfaces:
  - profiler_parser
sealed_surfaces:
  - profiler_parser
generation_apertures:
  - metric_mapping
integration_mode: readonly
benchmark_suite: pack_integrations
benchmark_targets: ["cpu_event_profiling"]
reference_runner: iree_cpu_events
workspace_keys: ["iree_cpu_events", "iree"]
expected_files: []
available_profilers: ["iree_cpu_events"]
metadata:
  profiler_kind: cpu_events

