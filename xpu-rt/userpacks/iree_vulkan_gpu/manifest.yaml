name: iree_vulkan_gpu
version: "0.1.0"
kinds: ["ProfilerPack"]
owned_surfaces:
  - profiler_parser
sealed_surfaces:
  - profiler_parser
generation_apertures:
  - metric_mapping
  - trace_correlation
integration_mode: readonly
benchmark_suite: pack_integrations
benchmark_targets: ["vulkan_gpu_profiling"]
reference_runner: iree_vulkan_gpu
workspace_keys: ["iree_vulkan_gpu", "iree"]
expected_files: []
available_profilers: ["iree_vulkan_gpu"]
metadata:
  profiler_kind: vulkan_gpu

