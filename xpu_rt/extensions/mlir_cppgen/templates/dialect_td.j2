//===- {{ prefix }}Dialect.td - {{ prefix }} dialect -------*- tablegen -*-===//
//
// Auto-generated by XPU-RT mlir_cppgen — do not edit manually.
//
//===----------------------------------------------------------------------===//

#ifndef {{ prefix | upper }}_DIALECT
#define {{ prefix | upper }}_DIALECT

include "mlir/IR/OpBase.td"

//===----------------------------------------------------------------------===//
// {{ prefix }} dialect definition.
//===----------------------------------------------------------------------===//

def {{ prefix }}_Dialect : Dialect {
    let name = "{{ dialect_name }}";
    let summary = "XPU-RT {{ prefix }} dialect.";
    let description = [{
        Auto-generated MLIR dialect for XPU-RT's {{ dialect_name }} IR.
        Generated from xDSL prototype definitions.
    }];
    let cppNamespace = "::{{ cpp_namespace }}";
{% if has_attrs %}
    let useDefaultAttributePrinterParser = 1;
{% endif %}
}

//===----------------------------------------------------------------------===//
// Base {{ prefix }} operation definition.
//===----------------------------------------------------------------------===//

class {{ prefix }}_Op<string mnemonic, list<Trait> traits = []> :
        Op<{{ prefix }}_Dialect, mnemonic, traits>;

#endif // {{ prefix | upper }}_DIALECT
